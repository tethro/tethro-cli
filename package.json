{
  "name": "tethro-cli",
  "version": "0.9.4",
  "description": "Tethro CLI — sandbox AI coding agents with microVM isolation, credential proxying, and audit logging",
  "bin": {
    "tethro": "./bin/tethro"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile bin/tethro --target node",
    "dev": "bun src/index.ts",
    "install-global": "npm link"
  },
  "dependencies": {},
  "license": "Apache-2.0"
}
